# NFO Tracker Environment Configuration
# ======================================
# Copy this file to .env and fill in the real values.
# .env must NOT be committed to git.
#
# Local dev: Expo will read these automatically.
# EAS builds: values come from EAS Secrets referenced in eas.json.
#
# To set EAS secrets, run:
#   npx eas secret:create --scope project --name supabase_url --value "<your-url>"
#   npx eas secret:create --scope project --name supabase_anon_key --value "<your-key>"
#   npx eas secret:create --scope project --name ors_base_url --value "<your-url>"
#   npx eas secret:create --scope project --name ors_api_key --value "<your-key>"

# ===================
# Supabase (required)
# ===================
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# ===================
# ORS / Routing Backend (required)
# ===================
EXPO_PUBLIC_ORS_BASE_URL=
EXPO_PUBLIC_ORS_API_KEY=
